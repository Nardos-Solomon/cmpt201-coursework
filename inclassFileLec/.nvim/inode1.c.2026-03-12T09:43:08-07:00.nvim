#include <unistd.h>
#include <stdio.h>
#include <sys/stat.h>

int main(int argc, char * argv[]){
  for (int i=1; i<argc;++i){
    struct stat s;
    int res= stat(argv[i], &s);
    if (res<0){
      fprintf(stderr,"stat %s",argv[i]);
      perror("");
      continue;
    }
  }
  return 0;
}
